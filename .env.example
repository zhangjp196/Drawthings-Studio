# ---- 数据目录 ----
# 数据库(app.db)、媒体文件、项目元数据均存放于此。
# DATA_DIR=./data

# 说明：LLM / DrawThings 的端点、模型、API Key 等配置已迁移到页面管理
# （/configs），按项目选用并存储于 SQLite，不再使用配置文件。
# 本应用直接调用真实服务（OpenAI 协议 LLM + Draw Things HTTP/gRPC），
# 运行前请先启动对应端点（见 README「运行前提」）。
